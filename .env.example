# Railway / production környezeti változók (példa)
# Másold .env fájlba lokális teszthez, vagy állítsd be a Railway Dashboardon.

NODE_ENV=production
PORT=3001

# Perzisztens adatok (Railway Volume mount után):
# DATA_DIR=/data/data
# UPLOADS_DIR=/data/uploads

# Fejlesztői teszt: szimulált „most” időpont (csak NODE_ENV !== production)
# PUBLISH_TEST_NOW=2026-06-22T15:00:00+03:00
